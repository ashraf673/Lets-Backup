package com.letsbackup.app.archive

object BackupFormat {
    const val FORMAT = "Let's Backup Archive"
    const val VERSION = 1
    const val MANIFEST = "manifest.json"
    const val CHECKSUMS = "checksums.sha256"
    const val EXTENSION = ".lb.zip"
}
